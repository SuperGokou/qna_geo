# Server only. Never use a VITE_ prefix for secrets.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
PORT=4317
HOST=127.0.0.1
# Required to bind to 0.0.0.0 for LAN access (at least 12 characters).
APP_PASSWORD=
SESSION_SECRET=
# Set to the HTTPS site origin behind a reverse proxy.
PUBLIC_ORIGIN=
